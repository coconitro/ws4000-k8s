# Security

Do not commit Kick stream keys, RTMP URLs, VNC passwords, or Ingress basic-auth
passwords to this repository. Use Helm `--set` or a gitignored `values-local.yaml`.

Report security issues via GitHub or Gitea issues on this repository.
